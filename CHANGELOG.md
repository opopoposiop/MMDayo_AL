# PPAL 更新履歴

現行版は`PPAL_2`です。`update/`にある`PPAL`は旧版であり、現行版とはモーフ構成が異なります。

## 2026-08-04 — PPAL_2

### 新規ファイル

- `PPAL_2.pmx`
- `PPAL_2.fxdayo`

### 変更内容

- コントローラーモーフを日本語名へ統一
  - `off`
  - `発光強度`
  - `ぼかし幅低減`
  - `光芒数`
  - `元画面輝度`
  - `AL-Z発光強度`
- 次のコントローラーモーフと機能を追加
  - `光芒角度` — 指定した角度だけ光芒を回転
  - `光芒長さ` — `-100`で長さ0、`0`で標準、`100`で2倍
  - `点滅周期` — 入力値を秒単位の周期として使用。正値は滑らかな波、負値は矩形波、`0`は点滅停止
- `PPAL_2.fxdayo`のコントローラー参照名を`PPAL_2.pmx`のモーフ名と一致させた
- 旧版の`PPAL.pmx`と`PPAL.fxdayo`を`update`フォルダーへ移動

### 互換性に関する注意

- `点滅周期`が正の場合は、発光倍率が約`0.2`～`1.0`の範囲で滑らかに変化します。
- `光芒数`は`0`～`9`に制限され、1方向につき正逆2本の光芒が生成されます。
- 光芒は方向数で明るさを正規化するため、方向数を増やしても極端に明るくなりにくい設計です。
