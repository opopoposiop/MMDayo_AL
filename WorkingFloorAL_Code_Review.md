`WorkingFloorAL_Code_Review.md` 
この2Dスクリーンスペースでの単純な上下反転（畳み込み反射）が幾何学的に正しく見えるのは、**「カメラが床に対して完全に水平（ピッチ角・ロール角がゼロ）」のときだけ**です。
MMD等のカメラワークで俯瞰（上から見下ろす）やアオリ、またはカメラの傾き（ロール）が発生すると、遠近感（パースペクティブ）の消失点がずれるため、反射絵が激しく歪むか、位置が大きくズレて視覚的に完全に破綻します。

---

## 🛠️ 改善案・アクションプラン

### 1. 床マスクのキャッシュ化（最優先）

ピクセルシェーダーのループ内で何度も `SourceFloorMask` を再計算するのをやめ、**ピクセルシェーダーの最初で画面全体の床判定を1回だけ行い、ローカル変数にキャッシュ**してください。
可能であれば、前パスで「床マスクテクスチャ」を生成しておき、ループ内ではテクスチャを1サンプリングするだけにする（またはGバッファの法線 $Y \approx 1.0$ などの簡易判定に置き換える）と劇的に軽量化します。

### 2. グローバル `static` の初期化タイミング修正

動的な値から行列（`InvViewProjectionMatrixLocal` など）を構築したい場合は、グローバルスコープでの初期化をやめ、**ピクセルシェーダー（`PS`）関数の先頭、またはそれを利用するローカル関数の内部でローカル変数として初期化**するように書き直してください。

### 3. 近傍サンプリングのループ外への追い出し

SSRのループ内では、単に「最も尤もらしい衝突UV（`bestHitUV`）」と「その重み（`bestWeight`）」を記録するだけに留めます。
そして**すべてのループが完全に終了した後に、決定した `bestHitUV` に対して1回だけ `SampleNonFloorNeighborhood()` を実行する**構造に変更してください。これだけで計算負荷を数分の一〜数十分の一に削減できます。

```

```